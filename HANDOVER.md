# 인수인계서 — PR 사이트 문장 다듬기 작업

작성일: 2026-07-14
대상 폴더: `pr_site`
이어받는 사람(AI)에게: 먼저 `CLAUDE.md`를 읽고 이 문서를 읽으세요.

---

## 1. 이 작업이 뭔가

강기호의 게임 기획자 PR 사이트에 들어간 **한국어 문장을 전부 세부 조정**하는 작업입니다.
사이트를 새로 만드는 게 아니라, 이미 있는 문장을 블록 단위로 훑으며 작성자가 직접 고쳐 나갑니다.

## 2. 작업 방식 — 이 흐름을 반드시 지킬 것

블록 하나씩, 아래 순서를 반복합니다.

1. **AI가 기존 문장을 그대로 보여준다.** 페이지 순서대로 한 블록씩. 라벨·제목·본문·태그를 항목별로 나눠 제시합니다. 한 번에 여러 블록을 쏟지 않습니다.
2. **작성자가 수정안을 준다.** 보통 항목 전체를 다시 적어서 줍니다. 안 바뀐 항목도 같이 적어 주는 편이라, **전체를 비교해서 실제로 달라진 부분만** 찾아 고쳐야 합니다.
3. **AI가 파일에 반영한다.** 그리고 다음 블록을 제시합니다.

지켜야 할 것:

- **AI가 임의로 문장을 고치지 않습니다.** 최종 문장은 작성자가 확정합니다. 제안을 요청받았을 때만 안을 냅니다.
- **작성자가 언급하지 않은 항목은 건드리지 않습니다.** 블록 안에서 일부만 수정안을 줬으면 나머지는 그대로 둡니다.
- **색 강조(`<span class="em">`, `<strong class="em">`)는 문구가 살아남으면 같이 유지합니다.** 작성자는 수정안을 평문으로 적어 주지만, 강조를 빼라는 뜻이 아닙니다. 강조 위치를 옮기거나 뺄 때는 명시적으로 말합니다.
- **문장이 사라지거나 앞뒤가 겹치면 짚어서 물어봅니다.** (예: 본문 4를 새로 쓰면서 본문 5와 내용이 겹쳤을 때 물어보고 뺐습니다.)
- 문체는 `CLAUDE.md`의 "글을 쓰거나 고칠 때 — 한국어 문체" 절을 따릅니다. 제안 문장을 낼 때 특히.

## 3. 지금까지 반영한 것

### 구조 변경 (문장 외)

- **`ai.html` 새로 만듦.** AI 관련 작업을 모은 페이지입니다. `projects.html` 구조를 그대로 따랐고, 카드 세 장이 들어 있습니다 — 블룸 사이클(`case-bloom.html`), sub_brain(`case-subbrain.html`), AI 레드팀 운영(`case-redteam.html`).
- **상단 내비에 `AI` 추가.** `index` / `projects` / `about` / `contact` 네 페이지 모두 Projects와 About 사이에 넣었고, `ai.html`의 AI 링크에는 `class="active"`가 걸려 있습니다. 모바일 메뉴(☰)는 같은 `#navlinks`를 쓰므로 자동으로 나옵니다.
- **`site.css`의 `.home-nav`를 2열 → 3열로.** 홈 카드가 세 장이 되어서입니다. (`grid-template-columns:repeat(3,1fr)`, 860px 이하는 1열 유지)
- **블룸 사이클 카드를 `projects.html`에서 뺐습니다.** AI 페이지 전용으로 옮겼고, 카드 문구는 그대로 유지했습니다. Projects에는 셔블·영산 두 장만 남았습니다.

### 문장 수정 (블록 순서대로)

| 블록 | 위치 | 반영 내용 |
|---|---|---|
| 1 | `index.html` 히어로 | 헤드라인 옆 캐릭터 GIF(`hero-art`) 삭제. 헤드라인 "잡학다식한 **기획자**, 강기호입니다."("시스템" 뺌). 리드 끝맺음 "팀원과 공감하는 개발자가 되겠습니다."로 교체하고 색 강조를 그 문장으로 옮김 |
| 2 | `index.html` Explore | 카드 2장 → 3장(Projects·AI·About). 설명 "두 갈래" → "세 갈래". 카드 문구 전부 교체 |
| 3 | `projects.html` 헤더 | 설명 → "제가 직접 만들거나 참여했던 게임 프로젝트들입니다." (제목·라벨 유지) |
| 4 | `projects.html` 카드 | 블룸 사이클 카드 제거 (위 구조 변경 참고) |
| 5 | `about.html` 오프너 | 본문 2 교체(보상의 가치까지 분석한다는 내용 추가). 본문 1·3, 제목 유지 |
| 6 | `about.html` 1.0 Origin | 본문 2 "설계 됐는지" 띄어쓰기. 본문 3 두 문장을 한 문장으로 합침. 본문 4 전면 교체(기획자가 되고 싶다는 확신이 선 과정). **본문 5 삭제**(새 본문 4와 내용이 겹쳐서) |
| 7 | `about.html` 2.0 Curiosity | 본문 2의 마지막 문장(`"잡학다식한 기획자"라는 말은…`) 삭제. 나머지 유지 |
| 8 | `about.html` 2.5 SWOT 머리말 | 유지(수정 없음) |
| 9 | SWOT S(강점) | 항목 1 → "위키·영상·인터뷰를 **조사, 수집하는** 습관이…" |
| 10 | SWOT W(성장 영역) | 항목 2 화살표 뒤 → "배우는 입장에서의 수용적인 태도로 조직문화를 빠르게 흡수" |
| 11 | SWOT O(기회) | 소제목 → "OPPORTUNITIES · 내가 회사에 **제공할 수 있는 것**" |
| 12 | SWOT T(위협) | 항목 1 "가장 어려움" 삭제, 끝에 "그러면 일할 의욕이 떨어짐." 추가. 항목 2 → "근거 있는 비판은 자기개선의 좋은 재료." / 끝 "다음 행동을 잡기가 어려움" |

## 4. 지금 걸려 있는 것 — SWOT 위협(T) 항목 1 (미해결)

**여기부터 이어받으면 됩니다. 가장 중요한 미결 사안입니다.**

현재 `about.html`에 들어가 있는 문장 (아직 교체 안 됨):

> **의도가 명확하지 못한 업무 지시**
> 왜 만드는지·누구를 위한 건지 설명 없이 기능만 떨어지는 지시. 의도를 분해해 설계로 옮기는 게 핵심 강점인데, 의도 없이 기능만 떨어지면 분해할 게 없어 단순 구현으로 줄어듦. 그러면 일할 의욕이 떨어짐.

**문제:** 작성자가 모의면접에서 이 항목 때문에 걸렸습니다. 면접관이 "직장생활을 하다 보면 부정확한 업무 지시가 분명히 있을 텐데 어떻게 대응할 거냐"고 물었고 말문이 막혔습니다. 즉 이 문구가 **꼬리물기 질문을 부릅니다.**

**시도했고 반려된 것:**

1. "되묻는다"는 대응을 문구 안에 넣어 질문을 차단하는 방향 → **반려.** "꼬리물기를 막으려는 의도가 너무 투명하게 보인다"는 이유.
2. 대응을 일하는 방식으로 자연스럽게 흘려 넣은 완화판 2안 → **반려.** 같은 이유.
3. 소재를 바꾼 3안(결과가 돌아오지 않는 작업 / 검증 없이 넘겨야 하는 구조 / 의견을 주고받을 수 없는 자리) 제시 → **작성자 답변 대기 중.** 직전에 "아예 다르게 가야겠다"고 했으므로, **"지시"라는 틀을 완전히 버린 새 소재**로 가야 합니다.

**이어받을 때 유의:**

- 제목도 새로 답니다. "의도가 명확하지 못한 업무 지시"를 유지할 필요 없습니다.
- T 항목 2가 "근거 없이 깎아내리는 피드백"이므로, **피드백·근거 소재와 겹치지 않아야** 합니다.
- 면접에서 "그럼 그런 상황에선 일 못 하냐"로 되받힐 여지가 없어야 하고, 동시에 방어하려는 티가 나면 안 됩니다.
- SWOT 항목 문체는 종결이 `~음 / ~됨 / ~편`인 짧은 개조식입니다. 이 톤을 지키세요.
- 참고할 작성자 성향: 위키·인터뷰를 파는 수집가 기질, 시스템이 왜 그렇게 짜였는지 뜯어보는 재미, 수치를 직접 여러 번 돌려보며 손맛을 맞추는 편, 자기 설계에 확신이 강한 편(W 항목).

## 5. 다음에 할 것 — 남은 블록

`about.html`부터 순서대로 이어가면 됩니다. 블록 13을 제시했으나 작성자 답변을 못 받은 상태입니다.

- **블록 13 (제시함, 답변 대기)** — `about.html` 3.0 Game Experience 머리말: 라벨 / 제목("경험의 자산이 된 게임들.") / 안내 문구
- 3.0 슬라이더 게임 6종의 `data-desc` 설명 (할로우 나이트, 데스티니, 몬스터 헌터, 라오어, 스도리카, 딥 락 갤럭틱)
- 4.0 Experience — 재미요소 아코디언 카드 2장(적을 살아 있는 존재로 / 이동 자체가 재미가 된다)
- 5.0 Skills — 숙련도 라벨, 평가 기준 툴팁, 하단 본문
- 6.0 AI Skills — 안내 문구, 스킬 카드 3장, sub_brain 소개, 하단 본문 2문단
- 7.0 Career — 안내 문구, 타임라인 4개 항목
- 8.0 Credentials — 카드 3장
- `contact.html` 전체
- `ai.html` 헤더 문구 (제목 "AI로 만든 게임과 AI 개발 환경." / 설명) — AI가 초안으로 쓴 문장이라 작성자 검토가 필요합니다
- 케이스 스터디 상세: `case-shovel` / `case-yeongsan` / `case-bloom` / `case-subbrain` / `case-redteam`
- 각 페이지 `<meta name="description">`·og 태그 — 본문을 고치면 여기도 어긋납니다. 마지막에 한 번 훑을 것

## 6. 확인 안 된 것

- 작성자가 `ai.html`에서 **AI 페이지의 active 표시가 안 된다**고 했습니다. 확인해 보니 `ai.html` 24번 줄에 `class="active"`가 정상적으로 들어가 있고, `site.js`는 active를 건드리지 않으며, `site.css`의 `nav.links a.active` 규칙(청록색 + 밑줄)도 정상입니다. **브라우저 캐시로 추정**해 강력 새로고침(Ctrl+F5)을 안내했으나 결과를 못 들었습니다. 여전히 안 되면 화면 캡처를 받아 다시 보세요.
- `images/redteam.png`, `images/sub-brain-graph.png`가 폴더에 없습니다. `onerror` 대체 처리가 있어 깨지진 않지만, 카드에 이미지 자리 텍스트가 뜹니다.

## 7. 손대면 안 되는 것 (`CLAUDE.md` 요약)

- 색·여백·모서리는 CSS 변수로만. 새 hex·px 직접 입력 금지
- 메인 4페이지는 `site.css`, 케이스 페이지는 `case.css`
- `onerror` 이미지 대체 처리 삭제 금지
- 빌드 도구·프레임워크·npm 새로 들이지 않기
- 한글 본문의 `word-break:keep-all` 유지
