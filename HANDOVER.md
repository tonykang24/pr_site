# 인수인계서 — PR 사이트 문장 다듬기 작업

작성일: 2026-08-13 (2026-07-14판 전면 교체)
대상 폴더: `pr_site`
이어받는 사람(AI)에게: **먼저 `CLAUDE.md`를 읽고 이 문서를 읽으세요.** 이 문서가 `CLAUDE.md`와 어긋나면 `CLAUDE.md`가 우선입니다.

이 판을 새로 쓴 이유는 스킬·플러그인 업데이트로 세션을 옮기기 때문입니다. 이전 판(2026-07-14)은 그 뒤 섹션이 삭제·재번호되고 표기 규칙이 새로 확정돼 대부분 낡았습니다. 이 문서만 보면 됩니다.

---

## 1. 이 작업이 뭔가

강기호의 게임 기획자 PR 사이트에 들어간 **한국어 문장을 전부 세부 조정**하는 작업입니다. 사이트를 새로 만드는 게 아니라, 이미 있는 문장을 블록 단위로 훑으며 **작성자가 직접 고쳐 나갑니다.**

빌드 없는 순수 HTML/CSS/JS 정적 사이트입니다. 페이지 10개 — `index` · `projects` · `ai` · `about` · `contact` · 케이스 5종(`case-shovel` · `case-yeongsan` · `case-bloom` · `case-subbrain` · `case-redteam`).

## 2. 작업 방식 — 이 흐름을 반드시 지킬 것

블록 하나씩, 아래를 반복합니다.

1. **AI가 기존 문장을 그대로 보여준다.** 페이지 순서대로 한 블록씩. 한 번에 여러 블록을 쏟지 않습니다.
2. **작성자가 수정안을 준다.** 보통 항목 전체를 다시 적어서 줍니다. 안 바뀐 항목도 같이 적어 주는 편이라, **전체를 비교해 실제로 달라진 부분만** 찾아 고쳐야 합니다.
3. **AI가 파일에 반영하고 저장한다.** 그리고 다음 블록을 제시합니다.

### 블록 제시 형식 (2026-08-13 작성자 지시로 확정)

**콘텐츠 블록 단위로 나눕니다. 본문은 문단별로 쪼개지 말고 한 덩어리로 보여 주세요.**

```
① 섹션 라벨      → 2.0 Curiosity
② 제목           → (<br />는 줄바꿈으로 표시)
③ 본문           → 빈 줄이 문단 나눔(<p> 하나). 전체를 한 블록으로.
④ 태그/기타      → 있으면
```

색 강조는 본문 안에 `〔 〕`로 표시해 보여 주고, 작성자가 고쳐 줄 때도 `〔 〕`로 위치를 받습니다. 문단 수를 늘리거나 줄여도 그대로 반영합니다.

### 지켜야 할 것

- **AI가 임의로 문장을 고치지 않습니다.** 최종 문장은 작성자가 확정합니다. 제안은 요청받았을 때만 냅니다.
- **작성자가 언급하지 않은 항목은 건드리지 않습니다.**
- **색 강조(`span.em` / `strong.em` / `b.em`)는 문구가 살아남으면 같이 유지합니다.** 작성자는 수정안을 평문으로 주지만 강조를 빼라는 뜻이 아닙니다.
- **문장이 사라지거나 앞뒤가 겹치면 짚어서 물어봅니다.** 제안했던 문장이 다른 문장과 충돌하면 그대로 넣지 말고 먼저 보고하세요(2026-08-13에 실제로 두 번 발생).
- **예외 — 맞춤법·띄어쓰기·표기 통일은 작성자가 자동 조정을 위임했습니다.** 고친 뒤 한 줄로 알리기만 하면 됩니다.

### 파일 반영 절차

`device_stage_files`로 스테이징 → `/tmp` 등 쓰기 가능한 곳으로 복사해 수정 → `SendUserFile`로 file_uuid 받기 → `device_commit_files`로 `pr_site` 폴더에 커밋. `/mnt/user-data/uploads/`는 읽기 전용입니다.

## 3. 문체 기준

**`pr_site/CLAUDE.md`의 "글을 쓰거나 고칠 때 — 한국어 문체" 절이 기준 그 자체입니다.** 참조할 다른 문서나 스킬은 없습니다.

- 근거를 한 문장에 길게 쌓고 짧은 판정으로 닫습니다. 인위로 끊어 균질하게 만들지 않습니다
- 1인칭 평가 종결(~느꼈습니다·~아쉬웠습니다), 괄호 보충(용어 즉석 정의)
- 청자는 대학생. 문장 구조는 평이하게
- "~가 아닌 ~입니다", "~것 같습니다" 같은 AI 정형 문장과 헤지 회피
- 한자어를 우리말로 바꾸지 않음(뜻이 같아도 전문성이 떨어져 보임). 게임·시스템 전문 용어도 그대로 — 2026-08-14 작성자 지시로 종전의 "우리말 우선" 규칙 삭제
- 마케팅 톤 금지. 효과를 감정·포부로 선언하지 말고 작동·조건·결과로

**기획서 문체 스킬(`doc-style`)의 규칙을 사이트 문장에 적용하지 마세요.** 그쪽의 "한 문장 한 결정"은 위 첫 규칙과 정면으로 어긋납니다.

> 스킬 환경 메모: 예전에 문체 정본으로 쓰던 스킬은 없어졌습니다(2026-08-11 정리 완료). 이 세션 중에는 `public-prose-style` 스킬도 목록에서 사라졌습니다. **없는 스킬을 찾지 말고 `CLAUDE.md` 절만 쓰세요.**

## 4. 확정된 표기 규칙 (2026-08-13, 작성자 승인)

새 문장을 넣을 때도 이 표를 따릅니다.

| 항목 | 확정 표기 |
|---|---|
| 게임 영문 | `셔블 (Shovel)` · `영산 (Yeongsan)` · `블룸 사이클 (Bloom Cycle)` — 첫 글자만 대문자. **예외**: `case-shovel.html` 기획서 블록의 `SHOVEL 캐릭터 기획서` / `SHOVEL Character Design Document`는 실제 노션 문서명이라 유지, URL도 유지 |
| 팀 규모 | `2인 팀` · `5인 팀` (수식어가 앞). 1인은 `1인 작업`, 블룸만 `1인 작업 + AI 협업` |
| 영산 역할 | `몬스터 기획 · 레벨 디자인 · 스프라이트 · VFX` |
| 셔블 역할 | 목록 자리는 `기획 · 인게임 구현`, `case-shovel` 히어로만 `기획 · 아트 · 적 AI(FSM)` |
| 나열 구분자 | 라벨·메타 자리(역할 줄·태그·카드 부제·표)는 `·`. **본문 산문의 나열은 쉼표** (2026-08-14 작성자 확정). `/`는 어디서도 쓰지 않음. 한 문장 안에서 `·`와 쉼표가 섞이면 쉼표로 맞춤 |
| 수량 단위 | `가지` (종·개 폐기. 순수 셈만 `개`) |
| 학교명 | `경기게임마이스터고` |
| 수상 | `GIGDC 중·고등부 금상`. 좁은 태그 자리(projects)만 `GIGDC 금상` 예외. 풀네임 괄호는 `case-yeongsan` 수상 블록 한 곳에만 |
| **제목** | **모든 제목에 마침표를 붙이지 않습니다** (`sub-title` · `sec-title` · `case-title` · index h1). 물음표는 유지 |
| **라벨** | **라벨 자리에는 문장을 쓰지 않습니다** (2026-08-14 작성자 확정). 명사구로 씁니다 — `숫자로 위계를 만든다` → `숫자로 만든 위계`. 카드 라벨·항목 라벨·메타 슬롯 이름 전부 해당 |

### 케이스 5종 공통 틀

| 슬롯 | 확정 |
|---|---|
| 히어로 메타 슬롯 이름 | `역할` · `형태` · `도구`(기술 아님) |
| 첫 블록 제목 | `목적 / 기획 의도` |
| 그 안 왼쪽 카드 | `목적` / 오른쪽 `기획 의도` — 2026-08-14 작성자 지시로 부제(`· 무엇을 푸나`·`· 왜 점프 하나인가`)를 떼고 라벨만 남김. 나머지 케이스 4종도 같은 형태로 맞출 것 |
| 표 헤더 | 왼쪽 `항목` / 오른쪽 `값과 설명` (셔블의 `규칙` 표는 2026-08-14 페이지 간소화로 삭제됨) |
| 하단 이동 버튼 | `다른 작업: ○○ →` |

### meta·og 규칙

10개 페이지 전부 `description` · `og:title` · `og:description` · `og:type` · `og:locale`을 갖습니다.
**`og:description`은 그 페이지 `meta description`과 같은 문장으로 둡니다.** 본문을 고쳐 meta를 바꾸면 og도 같이 바꾸세요.

### 고치지 않기로 확정한 것

- `AI 스킬·플러그인`(라벨)과 `AI 스킬과 플러그인`(서술문)의 병용 — 쓰임이 갈려 있어 정상
- `<title>` 형식 차이 — 화면 eyebrow가 `Case Study`(게임 3종)와 `Tooling`(sub_brain·레드팀)으로 갈린 걸 따라간 의도된 구분
- `데미지` — 업계 표준어라 '대미지'로 안 고침
- `case-subbrain` 본문 `세 켜` / 표 `3켜` — 표는 숫자가 맞음

## 5. 진행 중인 작업 — 비유 표현 제거

**작성자 지시(2026-08-13):** 사이트 전 문장에서 비유적 표현을 없애고, 비유가 강제되는 문장은 같은 기능을 하는 다른 문장으로 대체한다.
작성자가 준 방향 예시 — "기획할 때 꺼내 쓸 재료가 많아집니다" → "넓은 분야의 지식은 기획할 때 도움이 됩니다".

후보는 사이트 전체 **90곳 이상**입니다.

### 경계 (작성자 확정)

1. **업계 표준어는 남긴다** — 진입 장벽, 손맛, 코어 루프, 블랙박스, 히트박스, 고아 페이지, "스크립트가 죽는다"
2. **게임 안에서 실제로 일어나는 일을 가리키는 규칙 이름은 그대로** — 블룸 사이클의 "키워드가 자란다"·"바람을 타고 퍼진다", 영산의 "빙의"
3. **진행은 페이지별로 끊어서** — index → projects → ai → about → contact → 케이스 5종

### 작성자 판정 경향 (중요)

**작성자는 약한 비유·굳어진 관용구에는 손대지 않습니다.** index·projects·ai 세 페이지는 제안을 냈으나 전부 "유지"로 판정됐습니다. 따라서 `굴리다`(8곳), `머릿속에`, `눈에 들어왔습니다` 같은 것들은 그대로 남아 있습니다. **제안할 때 강한 비유와 사어화된 관용구를 나눠서 제시하고, 후자는 "유지 권장"으로 표시하세요.**

### 반영 완료

- **about 머리말** — `맞물려 돌아가는`→`서로 영향을 주고받는`, `마음이 갑니다`→`관심이 큽니다`, `자리/곳/거기` 제거, `멈춰 서서`→`그만두려다`
- **about 1.0 Origin** — 작성자가 전문을 직접 재작성해 줌. 제목 `게임 플레이가 어려운 환경`, 1문단 전면 교체, 2문단은 손대지 않음(`화면 너머의`·`설계에 더 끌리는 쪽` 비유를 작성자가 유지 판정), 3문단 `경기게임마이스터고로의 진학을 결정하게 되었고 … 두루 배우고, 한 게임이 어떻게 완성되는지를 경험했습니다`, 4문단 끝 `되기로 결정했습니다`. **3문단의 색 강조가 사라져 이 섹션 강조는 2문단 하나뿐입니다.**

## 6. 지금 위치 — 여기서 이어받으면 됩니다

**`about.html` 2.0 Curiosity 블록을 제시했고, 작성자 수정안을 기다리는 중입니다.**

현재 파일에 들어 있는 내용은 이렇습니다.

- ① 라벨 `2.0 Curiosity`
- ② 제목 `정보를 모으는 게,` / `그냥 즐겁습니다`
- ③ 본문 4문단 — 강조 2곳(둘 다 `span.em`): `〔그 이유를 찾는 일이 곧 제 공부가 됩니다.〕` · `〔기획할 때 꺼내 쓸 재료가 많아집니다.〕`
- ④ 태그 `나무위키` · `해외 위키` · `정보 요약 영상` · `게임 개발자 인터뷰`

이 블록에 작성자가 예시로 든 `재료`(2곳)와 `꺼내 쓸`이 있고, `한 게임을 파다 보면 옆 게임으로 새고, 또 다른 갈래로 빠집니다`, `한 분야에만 갇히지 않고`, `맞물려 돌아가는`(머리말에서 이미 바꾼 표현이 여기 남아 있음)도 있습니다.

### 남은 큐

`about.html` 3.0 Game Experience(슬라이더 5종 `data-desc` 포함) → 4.0 Skills → 5.0 AI Skills → 6.0 Career → 7.0 Credentials → `contact.html`(`재료로 쓰겠습니다`가 여기) → 케이스 5종(셔블 · 영산 · 블룸 · sub_brain · 레드팀).

`index` · `projects` · `ai`는 이번 비유 제거 라운드에서 "유지" 판정을 받았으니 건너뜁니다.

## 7. 작성자 판단 대기 목록

| 항목 | 상태 |
|---|---|
| `about.html` `2023 졸업`(타임라인)과 `2023년 졸업`(Credentials) 불일치 | 추천은 `2023년 졸업`. 미결 |
| 보조용언 붙여쓰기 `쌓아두는`(about) / `모아 두는`(about·subbrain) | 어법상 둘 다 허용. 통일 여부는 취향 |
| `about.html` 맨 위 리드(`about-opener` h2)의 마침표 | 제목 마침표 제거에서 제외함(두 문장짜리 리드라 빼면 문장이 끊김). 뺄지 미결 |
| GIGDC 풀네임이 `case-yeongsan` 본문 한 곳뿐 | about만 보고 나가는 사람은 뜻을 모름. about 타임라인 첫 등장에 넣을지 미결 |
| `case-shovel` 히어로 역할 표기 | 해소(2026-08-14) — 작성자 확인: `적 AI(FSM)`이 곧 인게임 구현. 누락 아님, 현행 유지 |
| `ai.html` 헤더 문장 | AI가 초안으로 쓴 문장이라 작성자 검토 필요 |
| `case-shovel` 히어로 리메이크 상태 배지 | 2026-08-14 히어로 블록 검토 때 "전부 유지" — 배지 없이 현행 유지. 다시 달려면 별도 지시 필요 |
| `case-shovel` 노션 기획서 블록 캡션 | 해소(2026-08-14) — PDF 블록 삭제로 노션 일원화, 캡션은 "위에서 다룬 규칙과 수치는 이 기획서에 전부 정리돼 있습니다"로 교체 |
| 영문 `Game System Designer` | 한글 "시스템 기획자"는 뺐는데 영문은 메인 4페이지 `<title>`·`og:title`과 index eyebrow에 남아 있음. 뺄지 미결 |

### 2.5 SWOT 섹션 삭제 (2026-08-14 작성자 지시)

`about.html`의 2.5 Self-Analysis(SWOT) 블록을 통째로 지웠습니다. 안내 문구·범례·S·W·O·T 카드 10개, 102줄입니다. 삭제에 딸린 처리는 이렇습니다.

- 이 섹션을 가리키던 내부 링크는 없었습니다(`#self-analysis` 참조 0건). 슬라이더·`.reveal`·`data-*`는 그대로입니다.
- `about.html`의 `meta description`과 `og:description`에서 `강점,`을 뺐습니다. SWOT이 없어져 사실과 어긋나기 때문입니다.
- `site.css`의 `swot-*`·`sl-*` 규칙이 안 쓰이게 됐습니다. 지우지 않고 뒀습니다. 되살릴 수 있고 화면에 영향이 없습니다.
- 절 번호는 소수점이라 뒤 섹션(3.0~7.0) 재번호가 필요 없습니다.

## 8. 사이트 상태 — 2026-08-13 전수 QA 결과

**통과:** HTML 파싱·태그 균형 10/10, 게임 슬라이더 5항목 `data-*` 무결, `site.js` 의존 id·data 전부 존재, 내부 링크 전부 실존, 로컬 이미지 20개 전부 `onerror` 보유, meta·og 규칙 준수, `lang="ko"` 10/10, CSS 미정의 클래스 0, `.reveal` 훅 전 섹션 부착, 표기 규칙 잔존 0건.

**미해결 (문장 작업과 분리 가능):**

1. **이미지 2개가 실제로 없습니다** — `images/sub-brain-graph.png`(about 5.0 · ai · case-subbrain 3곳), `images/redteam.png`(ai · case-redteam 2곳). `onerror`가 받아 주지만 화면에 `이미지 자리 —` 대체 텍스트가 뜹니다. about 5.0 본문은 "오른쪽은 …옵시디언 그래프 뷰입니다"라고 이미지를 가리키고 있어 특히 눈에 띕니다.
2. **안 쓰이는 이미지 2개** — `images/sdorica.png`(슬라이더에서 스도리카를 뺀 뒤 남음), `images/shovelhopping.gif`
3. **색 강조 태그가 세 형태** — about은 `span`(17)·`strong`(4), 케이스는 `span`·`b` 혼용. `b`·`strong`은 굵어지고 `span`은 색만 입어서, 같은 성격의 강조인데 페이지마다 굵기가 다릅니다.
4. **`projects.html` meta description이 사실과 다릅니다** — 블룸 사이클·플레이 빌드를 약속하는데 실제 페이지는 셔블·영산 카드 둘뿐입니다. 2026-08-13에 고칠지 물었고 작성자가 "유지"를 골랐습니다. `CLAUDE.md`의 파일 구조 설명도 같이 낡았습니다.
5. `ai.html`의 AI 링크 active 표시가 안 보인다는 예전 보고 — 코드는 정상이고 브라우저 캐시로 추정했으나 확인 못 받음.

## 9. 손대면 안 되는 것 (`CLAUDE.md` 요약)

- 색·여백·모서리는 CSS 변수로만. 새 hex·px 직접 입력 금지
- 메인 5페이지는 `site.css`, 케이스 페이지는 `case.css` (같은 토큰 이름인데 값이 다름 — `--maxw` 1120px / 880px)
- 게임 슬라이더의 `#gameTrack` · `#gameDetail` · `#gameSlider`와 각 `.gitem`의 `data-tag` · `data-title` · `data-desc` 건드리지 않기
- 이미지 `onerror` 대체 처리 삭제 금지
- `.reveal` 등장 훅은 새 섹션에도 달기
- 한글 본문의 `word-break:keep-all` 유지
- 빌드 도구·프레임워크·npm 새로 들이지 않기

## 10. 대량 수정 뒤 QA 체크리스트

HTML 구조를 바꾸거나 본문을 대량 교체했으면 저장 전에 확인하세요.

- 수정 전후로 `id` · `data-*` · `href` · `src` · `class` 목록이 같은지
- 태그 균형(파서로 확인)
- `.gitem` 개수와 각 `data-*` 속성 유지
- 내부 링크가 실제 파일을 가리키는지
- `meta description`을 고쳤으면 `og:description`도 같이 고쳤는지
- 새로 넣은 제목에 마침표가 없는지
