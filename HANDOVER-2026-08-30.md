# 인수인계서 — PR 사이트 문장 다듬기 (2026-08-30 세션)

이어받는 AI에게: 읽는 순서는 `CLAUDE.md` → `HANDOVER.md` → `HANDOVER-2026-08-21.md` → `HANDOVER-2026-08-22.md` → 이 문서다. 규칙은 앞 둘에 있고 나머지는 세션 기록이다. 어긋나면 `CLAUDE.md`와 `HANDOVER.md`가 이긴다.

이번 세션에 바뀐 것은 셋이다. `case-shovel.html`을 끝냈고, `case-yeongsan.html` 1차 정리를 했고, **git 사용을 그만두기로 했다.**

## 0. 먼저 — git을 쓰지 않는다

**2026-08-30 작성자 지시로 이 저장소를 GitHub에서 다루지 않는다. 로컬 파일만 고친다.**

- `git add`·`git commit`·`git push`를 하지 않는다. 잠금 파일 우회 절차도 이제 쓸 일이 없다
- 파일은 `device_bash`로 마운트 폴더에서 바로 고친다. 치환은 `python3` 힙독으로 `assert count==1`을 걸면 안전하다
- **큰 블록을 손보기 전에는 그 파일을 `.bak`으로 한 벌 복사한다.** 커밋이라는 되돌리기 지점이 없어졌다
- 채팅에 남기는 `기존` / `수정 후` 대비가 사실상의 복구 근거다. 1절 첫 규칙을 더 엄격히 지킬 것

작성자 쪽에 남은 숙제가 둘 있다. Claude가 처리할 수 없으니 물어보면 안내만 한다.

- **자동 동기화가 폴더 밖에서 돌고 있다.** `Auto sync: YYYY-MM-DD 20:00` 커밋을 만드는 무언가가 있는데 저장소 안에 훅은 없다. 끄지 않으면 커밋을 안 해도 변경이 GitHub로 올라간다
- **공개 저장소 히스토리에 전화번호가 남아 있다.** 파일에서 지웠다 되살린 것과 별개로 과거 커밋에 남는다. 저장소를 비공개로 바꾸거나 삭제해야 해결된다

세션 도중 `3a258d3`까지는 GitHub에 올라갔고 그 뒤 로컬 커밋 셋(`10c44de`·`fd263bf` 등)은 로컬에만 있다. 더 손대지 말 것.

## 1. 이번 세션에 선 규칙

### 신설 — 수정은 `기존` / `수정 후`를 나란히 보여준다

작성자 지시 두 번에 걸쳐 굳었다. "문장 수정 제안 전에 무조건 기존 버전의 문장을 보여줘", 이어서 "수정 후도 보여줘".

- 제안할 때도 반영을 보고할 때도 두 버전을 전문으로 싣는다. 어느 쪽도 자르지 않는다
- 객관식 보기를 낼 때는 질문 앞 본문에 기존 문장을 싣고 preview에 바뀔 문장을 넣는다. 작성자가 고른 뒤 반영 보고에서 다시 두 버전을 적는다
- 맞춤법·띄어쓰기처럼 자동 조정을 위임받은 항목도 마찬가지다

### 신설 — 손으로 물건을 다루는 은유를 쓰지 않는다

작성자가 셔블 회고 한 문단에서 세 표현을 한꺼번에 지목했다. 사유는 `푸는` 금지와 같다 — 한국어 화자가 실제로 쓰지 않는 꼴이라는 판단이다.

| 지목된 표현 | 작성자 말 | 대안 |
|---|---|---|
| `점프와 다운 스트라이크를 다른 키에 두었습니다` | "안 쓰는 표현" | 작성자 직접 수정 — `점프와 다운 스트라이크 키를 분리했었습니다` |
| `다운 스트라이크는 꺼내 보지도 않았습니다` | "들어본 적 없는 표현" | `쓰지 않았습니다` |
| `공격 키를 익히기 전에` | "안 쓰는 표현" | 원인을 시간 순서로 적은 꼴. `공격 키를 외우지 못해` |

방향은 작성자가 준 `분리했었습니다`가 보여준다. 배치·소지 은유 대신 그 행위의 직접적인 동사를 쓴다. 키는 두는 게 아니라 분리하고 합치는 것, 스킬은 꺼내는 게 아니라 쓰는 것이다.

**전체 적용은 하지 말 것.** 작성자는 같은 문단의 `누르던 버튼에서 공격이 따라 나오게`를 짚어 줬는데도 "그대로 두기"를 골랐다. `about`·`case-subbrain`에 남은 `보조로 둡니다`·`원본은 그대로 두고`·`불변으로 둡니다`·`바로 꺼내 쓸`·`스크립트에 박지 않고`도 손대지 않았다. 각 페이지 차례에 후보로만 올린다.

### 정정 — 완결 문장 금지의 범위

2026-08-22에 세운 "문단 끝 억지 마무리 문장 금지"를 이번에 사이트 본문까지 넓혔다가 **작성자에게 정정받았다.**

> 완결 문장을 만들려는 강박을 없애라는 거지 그걸 없애라고는 안 함.

걸리는 대상은 **Claude가 형식을 채우려고 지어내는 문장**이다. 작성자가 직접 쓴 마무리·교훈 문장은 대상이 아니다. 실제로 회고를 4문장으로 줄인 직후 작성자가 `이 경험으로 …알게 되었습니다`를 스스로 붙여 확정했다. **규칙을 근거로 작성자 문장을 빼자고 되묻지 말 것.**

### 확정 — 연결어미 뒤 쉼표를 넣지 않는다

`~는데`·`~지만`·`~해서`·`~해`·`~도록`·`~으니`·`~면서` 뒤 쉼표를 뺀다. 나열 쉼표는 그대로 둔다. `HANDOVER.md` 4절 표기 표에 행으로 올렸다.

**적용은 페이지 차례대로 한다.** 남은 목록은 5절에 있다.

## 2. `case-shovel.html` — 완료

여덟 블록이 전부 끝났다. 이번 세션에 세 곳을 고쳤다.

### 의도한 플레이어 경험 — 작성자가 새로 씀

Claude가 낸 A·B·C 초안은 폐기됐고 작성자가 직접 썼다.

기존
> 걷기를 빼면 플레이어는 빈 복도에서도 점프 호를 그리며 다음 착지 지점을 고릅니다. 멈추는 순간이 없으니 이동이 끊기지 않는 **통통 튀는 리듬**이 되고, 통로 하나를 지나는 일도 "다음엔 어디로 뛸까"를 고르는 선택의 연속이 됩니다.

수정 후
> 체력이 깎여도 회복은 다운 스트라이크로 적을 맞혀야만 나옵니다. 위험할수록 적에게서 멀어지는 대신 더 높이 뛰어 적의 머리 위로 뛰어들어야 하니 **물러서는 플레이가 성립하지 않습니다.** 체력이 낮을 때 소극적으로 플레이하게 되는 플레이스타일을 차단해서 플레이어가 과감하게 게임을 진행하고 게임이 루즈해지지 않도록 하고 싶었습니다.

반영 때 고친 것은 오타 `떄→때`와 쉼표 절 이음(`진행, 게임이` → `진행하고 게임이`) 둘뿐이다.

### 회고 — 여섯 번 고쳐 확정

가장 오래 걸린 블록이다. Claude가 쓴 문장을 작성자가 문장 단위로 지목하고 직접 고쳐 나갔다. 최종본은 이렇다.

> 처음에는 점프와 다운 스트라이크 키를 분리했었습니다. 그런데 테스트에서 조작 학습이 생각보다 느렸는데 지켜보니 플레이어들이 공격 키를 배운 이후에도 전투 상황이 닥치면 점프 키만 난사하는 모습을 보게 되었습니다. 그걸 보고 새로운 기능을 소개할 때마다 매번 조작키의 가짓수를 늘려 플레이를 복잡하게 만들기보다는 "유사한 기능의 조작을 하나의 키로 묶으면 학습 장벽을 낮추지 않을까?"라고 생각하게 되었습니다. 그래서 점프·차지 점프·다운 스트라이크를 스페이스바 하나로 묶었습니다. 이런 방법이 실제로 통해서 그 이후의 테스트에서는 대부분의 플레이어들이 제가 의도한 대로 게임을 플레이하게 되었습니다. 이 경험으로 우발적인 조작으로 일단 해보게 만드는 것이 플레이어를 학습시키는 데 효과적이라는 것과 단순한 조작의 중요성을 알게 되었습니다.

**이 문단에는 색 강조가 없다.** 작성자가 `[일단 해보게 만드는 것]`에 대괄호를 쳤다가 다음 판에서 뺐고, Claude가 넣었던 `<b class="em">`도 같이 지웠다. 다시 넣지 말 것.

중간에 들어왔다가 사라진 것 — 착지 리커버리 0프레임·코요테 타임·점프 버퍼, 그리고 `통통 튀는 리듬`. 한때 회고로 옮겼는데 작성자가 문단을 다시 쓰면서 통째로 빠졌다. **지금 사이트 어디에도 조작 관용과 플레이 질감을 말하는 대목이 없다.** 작성자는 이 상태를 알고 확정했다.

Claude가 짚었으나 작성자가 그대로 두기로 한 것 — `~하게 되었습니다`가 세 문장 연속으로 끝나는 것, 마지막 문장이 배운 점 두 개를 한 문장에 묶은 것.

### 설계 결정 3번 — 인과 한 단계 보완

오래 남아 있던 판단 대기 항목이 해소됐다. 근거는 작성자가 직접 줬다("수직 공간을 활용할 수 있게 되니까").

기존
> **레벨 디자인의 수직 폭** — 점프 이동을 확장해서 맵을 위아래로 넓게 쓸 수 있게 했습니다.

수정 후
> **레벨 디자인의 수직 폭** — 점프 이동을 확장해서 플레이어가 수직 공간을 활용할 수 있게 되니 맵을 위아래로 넓게 설계했습니다.

### 연결어미 쉼표 두 곳

`조작 학습이 생각보다 느렸는데,` → `느렸는데`, `해야 한다고 판단해,` → `판단해`. 뒤 문장은 이후 개작으로 사라졌다.

## 3. `case-yeongsan.html` — 여기서 이어받는다

작성자 지시는 "제안대로 1차로 바꾸고 2차로 블록 단위로 나한테 보여주면 직접 수정할게"였다. 1차는 끝났고 **2차 블록 검토가 다음 차례다.**

### 1차로 반영한 것

| 위치 | 기존 | 수정 후 |
|---|---|---|
| 목적 라벨 | 목적 · 무엇을 푸나 | 목적 |
| 기획 의도 라벨 | 기획 의도 · 왜 빙의인가 | 기획 의도 |
| 목적 본문 | …만들어 이 문제를 풉니다. | …만들어 이 문제를 없앱니다. |
| 2.5D 구성 | 그 한계를 풀려고 건물을 | 그 한계를 넘으려고 건물을 |
| 회고 | 2D 타일맵으로는 안 풀리는 요구가 쌓였습니다 | 2D 타일맵으로는 감당할 수 없는 요구가 쌓였습니다 |
| 목적 본문 강조 | `<span class="em">` | `<b class="em">` |
| 레벨 디자인 | `<b style="color:var(--text)">전투 웨이브 구간</b>` | `<b class="em">전투 웨이브 구간</b>` |
| 레벨 디자인 | 그대로 옮기려 했는데**,** 팀원이 | 그대로 옮기려 했는데 팀원이 |

라벨 부제를 뗀 것은 케이스 5종 공통 틀을 셔블에 맞춘 것이다. 색 강조는 `b class="em"`으로 통일하기로 했다.

### 2차 블록 검토 — 히어로까지 올렸고 회신 대기

**여기서 이어받으면 된다.** 작성자에게 히어로 블록을 보여주고 두 가지를 짚은 상태에서 대화가 다른 데로 넘어갔다.

히어로 부제 (기존, 그대로 있음)
> 무당이 가족을 잃고 부활해 복수를 시작한다. 적을 쓰러뜨릴 때마다 그 몸을 빼앗아 다음 길을 여는 빙의 시스템이 중심에 있는 액션 어드벤처입니다.

- **부제 안에서 문체가 갈린다.** 첫 문장은 `복수를 시작한다.`로 평서체, 뒤 문장은 `…액션 어드벤처입니다.`로 경어체다
- **부제와 콘셉트 블록이 같은 내용을 말한다.** 콘셉트 쪽 `조선의 무당이 의문의 인물에 의해 가족과 함께 살해당합니다. 혼자 부활한 무당은 복수할 대상을 찾아 길을 나섭니다`를 부제 첫 문장이 줄여 놓은 꼴이다. 케이스를 짧게 가져가는 기준으로 보면 한쪽을 덜 자리다

남은 블록 순서 — 플레이 영상, 콘셉트/이야기, 목적/기획 의도, 핵심 메커닉, 기여 4종(몬스터 기획·레벨 디자인·2.5D·VFX), 스케치, 인게임 화면, 회고, 수상.

### 영산에서 미리 잡아 둔 것

- **`풀다`가 한 곳 남았다.** 몬스터 기획 블록의 `"이 몸이면 다음 방을 풀 수 있겠다"`. 플레이어가 속으로 하는 말이고 스테이지를 깬다는 뜻의 관용이라, 블룸의 `반응이 풀린다`처럼 규칙 대상이 아닐 수 있다. 해당 블록에서 물을 것
- **연결어미 쉼표 세 곳이 남았다.** `몬스터별 동작·공격 패턴·약점을 설계해,`(몬스터 기획), `도트 톤에 맞는 가벼운 이펙트로 정리해,`(VFX), `저는 그 구성을 버리는 대신,`(레벨 디자인). 앞의 둘은 문장이 길어 쉼표가 호흡을 나누고 있으니 쉼표만 빼는 안과 문장을 끊는 안을 같이 낼 것
- **영산에 설계 수치가 하나도 없다.** 셔블·블룸에는 값이 있는데 금상 수상작이자 몬스터·레벨 기획을 맡은 영산은 서술만 있다. 몬스터별 패턴 구성과 웨이브 설계 수치를 넣으면 담당 범위가 증명된다. 작성자에게 물어볼 것

## 4. `contact.html` 변경 내역

이번 세션에 두 번 지웠다가 한 번 되살렸다. **최종 상태는 이렇다.**

- **전화 항목 — 있다.** 스팸 수집 우려로 지웠다가 작성자 지시로 되살렸다. `tel:01086636319` / `010-8663-6319`
- **GitHub 항목 — 없다.** 작성자 지시로 뺐다. 사유는 프로필 README가 기본 템플릿 그대로고 저장소에 설명이 없어서 링크가 손해라는 외부 검토였다. 사이트 전체에 `github.com` 링크가 없다
- 연락 수단은 이메일과 전화 둘이다

본문 문구는 이번 세션 전에 이미 바뀌어 있었다 — `근거 있는 지적일수록 반갑습니다. 주신 말씀은 다음 작업을 다듬는 재료로 쓰겠습니다.`

## 5. 연결어미 쉼표 미적용 목록

셔블은 끝났고 영산은 한 곳만 반영했다. 나머지는 각 페이지 차례에 처리한다.

짧아서 쉼표만 빼면 되는 곳 — `about` 스킬 카드 4곳(`기본은 다루지만,`), `bloom:141`(`통합해,`), `bloom:178`(`없으니,`), `bloom:186`(`더해,`), `redteam:94`(`않았는데,`), `redteam:96`(`봤지만,`), `subbrain:107`(`굴리는데,`), `subbrain:119`(`않도록,`), `subbrain:141`(`굴리면서,`).

문장이 길어 쉼표가 호흡을 나누는 곳 — `bloom:186`(`약해서,`), `bloom:186`(`몰리는데,`), `redteam:49`(`굴리는데,`), `redteam:86`(`있어서,`), `yeongsan`(`설계해,`·`정리해,`). 여기는 문장을 끊는 안을 같이 낸다.

손대지 않음 — `subbrain:45` 풀인용 `"모으는 건 잘했는데,<br />다시 못 찾았습니다."`. 쉼표가 줄바꿈과 짝이다.

## 6. 외부 검토를 받았을 때

작성자가 다른 세션·도구의 사이트 검토 결과를 붙여넣는 일이 있다. **그 내용을 그대로 믿고 작업하지 말 것.**

2026-08-30에 받은 검토 두 건은 절반이 저장소 최신본과 어긋났다. 이미 삭제된 SWOT 위협(T) 절, 이미 `2인 팀`으로 통일된 셔블 참여 형태, 이미 들어 있는 Credentials의 GIGDC 항목, 이미 다섯 페이지가 같아진 네비게이션을 문제로 지적했다. 배포본이 최신 상태를 반영하지 않은 탓으로 보인다.

항목마다 `grep`으로 로컬 파일을 확인하고 나서 실제로 남은 것만 처리한다. 그때 확인해 실제로 남아 있던 것은 이렇다 — 없는 이미지 탓의 플레이스홀더 노출, 이력서 PDF 부재, 영산 수치 부재.

## 7. 판단 대기 (누적)

`2023 졸업`/`2023년 졸업` 불일치, 보조용언 붙여쓰기 혼재, about-opener 마침표, GIGDC 풀네임 위치, `ai.html` 헤더 문장, 영문 `Game System Designer` 잔존, 7.0 이력 카드 형식 불일치, about 5.0의 `kiho-writing-ko` 잔존 여부, 4.0에서 뺀 `간단한` 되살릴지, about 6.0 고등학교 첫 문장 사실 확인.

셔블 설계 결정 3번은 이번에 해소돼 목록에서 뺐다.

## 8. 노션 기획서 (SHOVEL 캐릭터 기획서)

이번 세션에는 손대지 않았다. 미결은 `HANDOVER-2026-08-22.md` 3절 그대로다 — 표기 흔들림 7건과 내용 문제 2건(6.11 주석이 표와 어긋남, 2.3 기대효과와 6.5 연결). 반복 낱말 지적은 작성자가 "의도적인 부분"이라며 물렸으니 다시 올리지 말 것.

## 9. 자산 미해결

없는 이미지 — `sub-brain-graph.png`·`redteam.png`. 이 둘이 없어서 `ai.html` 두 곳과 `about.html` 한 곳에 `이미지 자리 — …` 텍스트가 화면에 드러난다. `genshin.png`·`blue-archive.png`도 없지만 `GI`·`BA` 이니셜 폴백이 나오도록 만들어져 있어 문제가 없다.

안 쓰이는 파일 — `sdorica.png`·`shovelhopping.gif`·`docs/shovel-character-v5.pdf`.

이력서 PDF가 사이트 어디에도 없다. `docs/`에 있는 것은 블룸 사이클 제안서 하나뿐이다.
