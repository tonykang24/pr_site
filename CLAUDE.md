# CLAUDE.md

이 파일은 Claude(또는 다른 AI)가 이 프로젝트를 작업할 때 따라야 할 지침입니다.
새 작업을 시작하기 전에 먼저 읽어 주세요.

## 이 프로젝트가 뭔가

강기호의 개인 포트폴리오 사이트입니다. 직무는 게임 시스템 기획자입니다.
빌드 과정이 없는 정적 사이트라서, HTML 파일을 브라우저로 바로 열면 그대로 동작합니다.
번들러나 프레임워크, npm 의존성은 쓰지 않습니다.

- 언어: 한국어 (`<html lang="ko">`)
- 기술 스택: 순수 HTML + CSS + 바닐라 JS
- 외부 의존성: Pretendard 폰트(jsDelivr CDN), YouTube 임베드, 라이브 데모 링크 한 개
- 배포: 정적 파일 그대로 호스팅 (별도 설정 파일 없음)

## 파일 구조

페이지는 HTML 다섯 종류입니다.

- `index.html` — 홈. 히어로 영역과 페이지 이동 카드(`.home-nav`)
- `projects.html` — 프로젝트 그리드, 케이스 스터디 요약, 영상 섹션
- `about.html` — 소개. `1.0`~`8.0` 번호가 붙은 섹션들(출신·호기심·게임 경험 슬라이더·역량·스킬·툴·경력 타임라인·자격)
- `contact.html` — 연락처
- `case-bloom.html`, `case-shovel.html` — 개별 케이스 스터디 상세

스타일과 스크립트는 이렇게 나뉩니다.

- `site.css` — 메인 4페이지 공용 스타일. `--maxw`는 1120px
- `case.css` — 케이스 스터디 전용 스타일. `--maxw`는 880px (본문이 더 좁음)
- `site.js` — 네비 스크롤 상태, 모바일 메뉴, 아코디언, 게임 슬라이더, 스크롤 등장 애니메이션
- `images/` — 게임 아이콘과 캐릭터 GIF. 파일명 규칙은 `images/README.txt`에 있음

케이스 스터디 페이지는 `site.css`가 아니라 `case.css`를 씁니다. 두 파일이 같은 토큰 이름(`--accent`, `--s5` 등)을 쓰지만 값이 일부 다르니, 케이스 페이지를 고칠 때는 `case.css`를 봐야 합니다.

## 디자인 토큰 — 하드코딩 금지

색·여백·둥근 모서리는 전부 `:root`의 CSS 변수로 정해 둡니다. 새 스타일을 넣을 때 숫자를 직접 쓰지 말고 변수를 쓰세요.

색:

- `--accent: #14b8a3` (청록) — 강조색. 링크 활성, 버튼, 포인트
- `--accent-soft: #5ad6c4`, `--accent-ink: #053b33`
- `--text: #16221e`, `--muted: #5f6c67`, `--muted-soft: #9aa6a1`
- `--bg: #ffffff`, `--bg-soft: #f4f8f7`, `--card: #ffffff`, `--line: #e5eae8`

여백은 8px 리듬입니다. `--s1`(4px)부터 `--s10`(120px)까지 단계가 있으니 그 안에서 고르세요.
둥근 모서리는 `--r-sm`(8px), `--r-md`(16px, 카드·이미지), `--r-lg`(22px, 게임 아이콘), `--r-pill`(999px, 칩·버튼·바)을 씁니다.

호버 효과는 사이트 전체가 한 패턴입니다. `translateY(-3px ~ -4px)` 살짝 띄우기 + `border-color:#bfe3da` + 청록 그림자. 새 카드를 만들면 이 패턴을 따라 주세요.

## 코드 컨벤션

- 클래스 이름은 짧은 케밥 케이스(`sec-title`, `cap-head`, `nav-card`)
- 본문 한글은 `word-break:keep-all`로 단어 단위 줄바꿈을 씁니다. 깨지 마세요
- 섹션 제목 위에는 `.sec-num`으로 작은 번호 라벨을 답니다 (예: `01`, `1.0`)
- 내부 페이지의 첫 섹션은 `.page-top` 클래스로 고정 네비 높이만큼 위 여백을 줍니다
- 이미지에는 `onerror`로 대체 처리를 넣습니다. 아이콘이 없으면 약자 타일이나 숨김으로 빠집니다. 이 안전장치를 지우지 마세요
- JS는 의존성 없는 바닐라입니다. 빌드 스텝을 새로 들이지 마세요

## 동작하는 인터랙션 (site.js)

기능을 고치거나 새로 붙일 때 아래 연결을 깨지 않도록 주의하세요.

- 스크롤 24px 넘으면 헤더에 `.scrolled`가 붙어 테두리·배경이 진해집니다
- 모바일 메뉴: `#navToggle` 버튼이 `#navlinks`에 `.open`을 토글합니다
- 역량 아코디언: `.cap` 안의 `.cap-head`를 누르면 `.cap-body`가 펼쳐집니다 (높이를 JS가 계산)
- 게임 슬라이더: `#gameTrack` 안의 항목을 JS가 복제해 무한 루프를 만듭니다. 각 `.gitem`은 `data-tag` / `data-title` / `data-desc` 속성으로 상세를 채웁니다. 상세 박스는 `#gameDetail`, 슬라이더 래퍼는 `#gameSlider`. 이 id와 data 속성 이름이 맞아야 동작합니다
- 등장 애니메이션: `.reveal` 클래스가 붙은 요소는 화면에 들어올 때 `.in`이 붙으며 나타납니다. 새 섹션에도 `.reveal`을 달면 같은 효과가 적용됩니다

## 게임 아이콘 추가하기

`images/README.txt`에 규칙이 적혀 있습니다. 요약하면, 정해진 파일명으로 정사각형 이미지를 `images/`에 넣으면 됩니다. 파일이 없을 때는 약자 타일이 자동으로 대신 나옵니다.

## 글을 쓰거나 고칠 때 — 한국어 문체

이 사이트의 모든 한국어 문장은 `kiho-writing-ko` 스킬의 규칙을 따릅니다.
포트폴리오 소개글, 케이스 스터디 본문, 게임 슬라이더 설명을 쓰거나 고칠 때는 그 스킬을 먼저 켜세요.

핵심만 적으면 이렇습니다.

- Kiho 문체 재현이 최우선입니다. 근거를 한 문장에 길게 쌓고 짧은 판정으로 닫습니다. 인위로 끊어 균질하게 만들지 않습니다
- 1인칭 평가 종결(~느꼈습니다·~아쉬웠습니다)과 괄호 보충(용어 즉석 정의)을 씁니다
- 청자는 대학생이라고 보고 씁니다. 문장 구조는 평이하게
- "~가 아닌 ~입니다", "~것 같습니다" 같은 AI 정형 문장 구조와 헤지를 피합니다
- 뜻이 같으면 한자어 대신 우리말을 씁니다. 게임·시스템 전문 용어(코어 루프·덱빌딩 등)는 그대로 둡니다
- 마케팅 톤을 쓰지 않습니다. 효과를 감정·포부로 선언하는 대신 작동·조건·결과로 적습니다

기획서나 발표 자료를 따로 만들 일이 생기면 `game-design-doc`(내용 구조)과 `ppt-layout-dreamnotion`(슬라이드 레이아웃) 스킬도 함께 씁니다.

## 작업할 때 지켜야 할 것

- 결과물은 사용자가 선택한 `pr_site` 폴더에 바로 저장합니다
- 메인 페이지를 고치면 `site.css`를, 케이스 페이지를 고치면 `case.css`를 봅니다. 헷갈리지 마세요
- 색·여백·모서리는 항상 CSS 변수로. 새 hex 값이나 px를 직접 박지 않습니다
- 호버·등장 애니메이션 패턴은 기존 것을 재사용합니다
- 빌드 도구, 프레임워크, npm 패키지를 새로 들이지 않습니다. 정적 사이트의 단순함을 지킵니다
- HTML을 크게 고친 뒤에는 브라우저에서 한 번 열어 네비·슬라이더·아코디언이 도는지 확인합니다
