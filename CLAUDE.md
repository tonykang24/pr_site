# CLAUDE.md

강기호의 게임 기획자 포트폴리오 사이트(`pr_site`) 작업 지침입니다.

파일 목록·CSS 변수 값·클래스 구조는 여기 안 적습니다. 폴더를 열어 보면 되니까요. 이 문서에는 **파일을 봐도 알 수 없는 것과, 모르고 건드리면 깨지는 것**만 적습니다.

## 제약

빌드가 없는 정적 사이트입니다. HTML을 브라우저로 바로 열면 그대로 동작합니다.

- 순수 HTML + CSS + 바닐라 JS. **번들러·프레임워크·npm 패키지를 새로 들이지 않습니다.**
- 외부 의존성은 셋뿐 — Pretendard 폰트(jsDelivr CDN), YouTube 임베드, 라이브 데모 링크 하나
- 언어는 한국어(`<html lang="ko">`), 배포는 정적 파일 그대로

## 건드리면 깨지는 것

- **CSS 파일이 둘입니다.** 메인 페이지는 `site.css`, 케이스 스터디 페이지는 `case.css`. 두 파일이 같은 토큰 이름을 쓰는데 값이 일부 다릅니다(`--maxw`가 1120px / 880px). 케이스 페이지를 고치면서 `site.css`를 보면 헛수고합니다.
- **게임 슬라이더는 id와 data 속성에 묶여 있습니다.** `#gameTrack` / `#gameDetail` / `#gameSlider`, 각 `.gitem`의 `data-tag` · `data-title` · `data-desc`. 이름이 하나라도 어긋나면 슬라이더가 죽습니다.
- **`.reveal`은 등장 애니메이션 훅입니다.** 붙은 요소만 스크롤 시 나타납니다. 새 섹션에도 달아야 같이 움직입니다.
- **이미지 `onerror` 대체 처리를 지우지 마세요.** 파일이 없을 때 약자 타일이나 숨김으로 빠지는 안전장치입니다.
- **본문 한글의 `word-break:keep-all`** — 단어 단위로 줄바꿈하려고 넣은 겁니다.

모바일 메뉴(`#navToggle` → `#navlinks.open`)와 헤더 스크롤 상태(24px 넘으면 `.scrolled`)도 `site.js`에 있습니다. HTML을 크게 고쳤으면 브라우저에서 한 번 열어 네비·슬라이더가 도는지 보세요.

## 스타일 — 값을 직접 박지 않기

색·여백·둥근 모서리는 전부 `:root`의 CSS 변수입니다. **새 hex나 px를 직접 쓰지 말고 변수를 쓰세요.** 실제 값과 용도 주석은 `site.css` 맨 위 `:root`에 있으니 거기서 고릅니다. 여백은 8px 리듬(`--s1`~`--s10`)입니다.

호버는 사이트 전체가 한 패턴입니다 — `translateY(-3px ~ -4px)` + `border-color:#bfe3da` + 청록 그림자. 새 카드도 이 패턴을 재사용합니다.

클래스 이름은 짧은 케밥 케이스(`sec-title`, `nav-card`). 섹션 제목 위에는 `.sec-num` 번호 라벨, 내부 페이지 첫 섹션에는 `.page-top`(고정 네비 높이만큼 위 여백).

게임 아이콘을 추가할 때는 `images/README.txt`의 파일명 규칙을 따릅니다.

## 글을 쓰거나 고칠 때 — 한국어 문체

이 사이트의 모든 한국어 문장은 아래 규칙을 따릅니다. 이 절이 문체 기준 그 자체입니다 — 다른 문서나 스킬을 찾지 마세요.
포트폴리오 소개글, 케이스 스터디 본문, 게임 슬라이더 설명을 쓰거나 고칠 때 여기를 먼저 읽습니다.

- 작성자 문체 재현이 최우선입니다. 근거를 한 문장에 길게 쌓고 짧은 판정으로 닫습니다. 인위로 끊어 균질하게 만들지 않습니다
- 1인칭 평가 종결(~느꼈습니다·~아쉬웠습니다)과 괄호 보충(용어 즉석 정의)을 씁니다
- 청자는 대학생이라고 보고 씁니다. 문장 구조는 평이하게
- "~가 아닌 ~입니다", "~것 같습니다" 같은 AI 정형 문장 구조와 헤지를 피합니다
- 뜻이 같으면 한자어 대신 우리말을 씁니다. 게임·시스템 전문 용어(코어 루프·덱빌딩 등)는 그대로 둡니다
- 마케팅 톤을 쓰지 않습니다. 효과를 감정·포부로 선언하는 대신 작동·조건·결과로 적습니다

**기획서 문체 스킬(`doc-style`)의 규칙을 사이트 문장에 적용하지 마세요.** 그쪽의 "한 문장 한 결정"은 위 첫 번째 규칙과 정면으로 어긋납니다. 기획서를 따로 만들 일이면 `game-design-doc`·`ppt-layout-dreamnotion`을 쓰되, 사이트 문장은 이 절입니다.

## 문장 작업 방식

**AI가 임의로 문장을 고치지 않습니다.** 최종 문장은 작성자가 확정합니다. 제안은 요청받았을 때만 냅니다. 자세한 진행 규칙은 `HANDOVER.md`에 있습니다.

예외 — 맞춤법·띄어쓰기·표기 통일은 작성자가 자동 조정을 위임했습니다. 고친 뒤 한 줄로 알리기만 하면 됩니다.

결과물은 `pr_site` 폴더에 바로 저장합니다.
