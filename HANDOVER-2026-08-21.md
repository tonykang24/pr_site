# 인수인계서 — PR 사이트 문장 다듬기 (2026-08-21 세션)

이어받는 AI에게: `CLAUDE.md` → `HANDOVER.md` 순서로 먼저 읽는 전제는 그대로다. 이 문서는 그 뒤에 붙는 이번 세션 기록이고, 규칙 자체는 전부 `HANDOVER.md`에 반영해 뒀다. 어긋나면 `CLAUDE.md`와 `HANDOVER.md`가 이긴다.

## 1. 이번 세션에 확정된 규칙 (전부 HANDOVER.md에 반영 완료)

**문체 정본 개정**

- `CLAUDE.md` 44행의 "뜻이 같으면 한자어 대신 우리말" 규칙을 **삭제**했다. 한자어를 줄이면 전문성이 매우 떨어져 보인다는 작성자 판단이다. 한자어는 그대로 두고 조사 겹침·명사구+되다·피동·호응 오류만 손본다.

**표기 규칙 (HANDOVER.md 4절)**

- **나열 구분자** — 라벨·메타 자리는 `·`, 본문 산문은 쉼표. 한 문장 안에서 섞이면 쉼표로 맞춘다.
- **라벨** — 라벨 자리에 문장을 쓰지 않는다. 명사구로 쓴다. 짧을수록 낫다(관형형보다 명사 결합 선호). 작성자가 `없앤 광역 카드 분류`를 `광역 카드 삭제`로 다시 줄인 게 기준점이다.
- **설계 결정 블록** — 라벨은 `설계 결정`으로 통일. 셔블·블룸·sub_brain 셋 다 적용했다.

**쓰지 않는 표현 (HANDOVER.md 4절 신설)**

- `~을/를 푸는`·`~을/를 푼다` — **영문 직역체다.** solve를 그대로 옮긴 꼴이고 한국인이 안 쓰는 형태라는 게 사유다. 단언이 세서가 아니므로 대상을 바꿔도 이 어형 자체를 피한다. 경계: 블룸의 `반응이 풀린다`는 게임 규칙 서술어라 해당 없음.
- `항상`·`모든`으로 예외 없음을 단언하기 — 반례가 있으면 사실 오류가 된다. 블룸의 `그 첫 칸은 항상 플레이어가 고른 카드`는 반례가 없어 통과다.

**Claude 답변 문체 (natural-korean 4차 개정에 반영, .skill 전달함)**

- 공간 은유 두 계열 금지 — `~하는 자리입니다`(place), `~가 안 서다`(stand). 작성자가 Claude 답변에서 직접 잡아냈다. `자리`는 물리적 위치에만 쓴다.

## 2. 파일별 반영 내역

**about.html**

- 2.5 SWOT 섹션 전체 삭제(102줄). `#self-analysis` 참조 0건이라 링크 손상 없음. meta·og description에서 `강점,` 제거. `site.css`의 `swot-*`·`sl-*` 규칙은 안 쓰이지만 남겨 뒀다. **약점을 스스로 밝히는 대목이 사라졌다** — 필요하면 6.0 Career 안내 문구로 대신 받을 수 있다고 제안해 뒀다.
- AI 문체 A급 14건 반영 — 머리말 3문단, Origin 1~4문단, Curiosity 1·3문단, 슬라이더 4종, 4.0 본문, 6.0 고등학교·영산. 가장 컸던 수정은 Origin 3문단(`경기게임마이스터고로의 진학을 결정하게 되었고 학업을 이수하면서` → `경기게임마이스터고에 진학해`).
- **2.0 Curiosity 블록 전면 교체** — 작성자가 새 원고를 직접 썼다. 제목 `자료 수집이 취미인 사람`, 3문단, 강조 2곳(`일종의 호더(hoarder; …) 기질` / `한 문제를 여러 시선에서 바라볼 수 있는 시선`).
- 4.0 Skills — C# 스크립트 중상 → **중하**(막대 80% → 40%, 기준 문구까지). Excel `중상 · 함수·VBA 활용`. 본문도 숙련도에 맞춰 낮췄다(`직접 만들 수 있습니다` → `찾아보면서 만들었습니다`).
- 5.0 AI Skills — `kiho-writing-ko` 카드 삭제(실패 사례라 뺀다는 작성자 판단), **40K 아미 빌더** 카드 신설(태그 `Web App`, netlify 링크 포함). 본문의 `그렇게 5.1.0까지 왔습니다`도 스킬명 없이 다시 썼다.
- 3.0 슬라이더에 **원신·블루 아카이브 추가**(총 7종). 이미지가 없어 `GI`·`BA` 약자 타일로 뜬다. `images/README.txt`에 `genshin.png`·`blue-archive.png`를 적어 뒀다.

**case-shovel.html — 12블록 → 8블록**

삭제: 규칙 표, 속도 막대, 구버전 PDF(iframe), 3축 카드, 영상 캡션, 노션 캡션.
남은 구성: 영상 → 노션 → 목적/기획 의도 → 핵심 루프 → 핵심 메커닉 → 설계 결정 → 의도한 플레이어 경험 → 회고.

- 히어로 리드 — `점프 중심의 캐릭터 디자인입니다.` (about 6.0도 `점프 중심의 메트로바니아 캐릭터`로 맞췄다)
- 목적/기획 의도 — 제목 `점프와 전투를 하나로`, 카드 라벨은 부제를 떼고 `목적` / `기획 의도`, 본문 둘 다 작성자 원고, 강조는 `친숙하면서도 새로운 경험`.
- 핵심 루프 — 라벨에서 화살표 제거, 설명 세 줄 작성자 확정본(`치면`→`공격하면`, `낙하 지점(공격 대상)`).
- 핵심 메커닉 — 라벨 `점프 온리 이동` → `점프 중심의 캐릭터 디자인`, 1번 항목에서 `항상`·`모든` 제거하고 대시·패링 포고를 확장 이동으로 명시.
- 설계 결정 — 수치 항목(25/50 타일) 삭제, 세 항목 전부 작성자 확정본, 강조 1곳.

**케이스 5종 공통** — 문장형 라벨 18곳을 명사구로 바꿨다(루프 카드 제목 9, 항목 라벨 9). 그중 다섯은 작성자 기준에 맞춰 한 번 더 줄였다.

**docs/shovel-character-v5.pdf는 이제 어디서도 안 쓰인다.** 작성자가 직접 지우면 된다.

**노션 기획서** — `SHOVEL 캐릭터 기획서`에서 표지 표의 `리비전`·`변경 이력` 두 행을 삭제했다(발표용이라 버전 이력 불필요).

## 3. 여기서 이어받으면 된다

**`case-shovel.html` 핵심 메커닉 블록을 새로 쓰는 중이다.** 지금 다섯 항목이 규칙 나열에 가까워서, 노션 기획서 6장을 읽고 다룰 만한 메커닉을 다시 고르기로 했다. Claude가 셋을 추천했고 작성자 회신을 못 받았다.

1. **점프 온리 이동** — 걷기 없음, 방향 입력은 공중에서만 유효. 지상에서는 좌우 반전만 되고 실제 이동은 다음 점프 궤적으로만 일어난다(기획서 6.2.1). 이 규칙이 없으면 나머지 항목을 설명할 수 없다.
2. **다운 스트라이크의 높이 = 공격력** — 낙차를 **입력 시점부터** 재기 때문에 같은 풀차지라도 정점에서 바로 누르면 강 구간, 늦게 누르면 중 구간이 된다(기획서 6.6.1). 언제 누를지까지 플레이어 판단으로 만든 규칙인데 현재 사이트 문장이 이걸 빠뜨렸다.
3. **점프 밟기 대 다운 스트라이크의 판정 폭 차이** — 0.8타일과 1.0타일(기획서 6.5). 밟기는 우연한 처리라 좁아도 되고, 확실히 잡으려면 다운 스트라이크를 쓰게 유도한다. 기획서에 "판정 폭 차이가 곧 위계의 물리적 표현"이라고 적혀 있다. 삭제한 25/50 타일 논점을 대신할 수 있다.

B급 후보로 행 타임(6.3), 패링 0.5초 active·쿨다운(6.7)도 정리해 뒀다. 회복·잔불은 `설계 결정` 블록과 겹치니 넣지 않는다.

## 4. 셔블에 남은 미결

- **설계 결정 세 번째 항목** — `레벨 디자인의 수직 폭 — 점프 이동을 확장해서 맵을 위아래로 넓게 쓸 수 있게 했습니다.` 사다리·엘리베이터를 빼면서 "왜 수직이 넓어지나"의 한 단계가 생략됐다. 라벨에 인과를 싣거나(`점프로 넓힌 수직 공간`) 본문에 한 조각을 되돌리는 안이 있다.
- `의도한 플레이어 경험`과 `회고` 두 블록은 아직 검토 전이다. 회고는 Claude가 쓴 문장 그대로다.

## 5. 다음 파일 순서

`case-yeongsan` → `case-bloom` → `case-subbrain` → `case-redteam` → `contact.html`.

각 파일에서 미리 잡아 둔 것:

- **영산·블룸 루프 설명에 연결어미 뒤 쉼표가 남아 있다** — `되니,` `단계라,` `번질지,`.
- **블룸은 `접근 / 설계 — 한 턴의 3단계`와 `핵심 루프`가 거의 같은 이야기를 한다.** 셔블 3축 블록과 같은 상황이라 통합·삭제를 검토할 것.
- **sub_brain의 `쓰는 법 — 넣기 · 묻기 · 점검`이 카드 제목 셋과 정확히 같은 말이 됐다.** 라벨을 `쓰는 법`으로 줄이면 셔블·블룸과 형태가 맞는다.
- **케이스 5종 공통 틀** — `목적 · 무엇을 푸나` 같은 부제를 떼고 라벨만 남기는 규칙을 셔블에만 적용했다. 나머지 넷도 맞춰야 한다.

## 6. 진행 방식 (이번 세션에서 굳은 것)

- **블록 하나씩 보여 주고 작성자 수정안을 받아 반영한다.** 형식은 `HANDOVER.md` 2절 그대로. 색 강조는 `〔 〕`로 주고받는다.
- **제안할 때 `〔 〕`를 강조 표시로만 쓴다.** 이번 세션에서 Claude가 바뀐 낱말 표시에도 같은 기호를 써서 작성자가 헷갈렸다. 변경 부분은 다른 방식으로 표시할 것.
- **판단이 필요하면 객관식 보기로 낸다.** 작성자가 명시 요청했고 결정이 빨라진다. 다만 지시가 짧아 범위가 애매할 때가 있으니(`swot항목 빼자`가 한 칸인지 섹션 전체인지), 되돌리기 비싼 작업은 보기로 되물을 것.
- **작성자 확정 문장 불가침은 풀렸다.** 본인이 자기 말투가 AI 문체에 옮았다고 판단해 직접 쓴 문장도 진단 대상에 넣었다. 확정 권한은 그대로 작성자에게 있다.
- **문체 학습은 계속한다.** 작성자가 제공하는 원고에서 관찰을 쌓아 프로젝트 메모리 `kiho-style-observations.md`에 적는다. 채팅 지시문은 학습 대상이 아니다. 케이스 4종을 마치면 스킬로 산출하기로 했다 — 지금은 규칙은 많은데 원고 표본이 적어 문체 재현이 반쯤만 된다.

## 7. 작업 환경 주의

- **파일 반영 절차** — `device_stage_files` → `/tmp` 복사 → 수정 → `SendUserFile` → `device_commit_files`. `/mnt/user-data/uploads/`는 읽기 전용이다.
- **git 잠금 파일** — 이 세션에서 git을 쓰면 `index.lock`·`HEAD.lock`이 남고 삭제 권한이 없다. `pr_site/.git/_stale_locks/`로 치워 뒀으니 작성자가 지우면 된다. git 작업이 잦아지면 데스크톱 앱에서 "이 작업 실행"으로 컴퓨터에서 다시 시작하는 편이 낫다.
- **체크포인트 커밋** — `dd82e64`가 about A급 반영 직후 지점이다. 그 뒤 커밋은 안 했으니 지금 작업 트리에 이번 세션 수정분이 전부 쌓여 있다. **이어받으면 `git status`부터 확인하고 커밋을 권할 것.**

## 8. 판단 대기 (HANDOVER.md 7절에도 있음)

`2023 졸업`/`2023년 졸업` 불일치, 보조용언 붙여쓰기 혼재, about-opener 마침표, GIGDC 풀네임 위치, ai.html 헤더 문장, 영문 `Game System Designer` 잔존, 7.0 이력 카드 형식 불일치, about 5.0의 `kiho-writing-ko` 잔존 여부(카드는 뺐고 본문 언급도 정리했으니 재확인만 하면 된다), 4.0에서 뺀 `간단한` 되살릴지.

**자산 미해결** — `images/sub-brain-graph.png`·`redteam.png`·`genshin.png`·`blue-archive.png` 없음(onerror로 동작). 안 쓰이는 `sdorica.png`·`shovelhopping.gif`·`docs/shovel-character-v5.pdf`.
