# 인수인계서 — PR 사이트 문장 다듬기 (2026-08-22 세션)

이어받는 AI에게: 읽는 순서는 `CLAUDE.md` → `HANDOVER.md` → `HANDOVER-2026-08-21.md` → 이 문서다. 규칙은 앞 둘에 있고 뒤 둘은 세션 기록이다. 어긋나면 `CLAUDE.md`와 `HANDOVER.md`가 이긴다.

이번 세션은 셔블 케이스 페이지 두 블록과 **노션 기획서 전수 교정**을 했다. 사이트가 아니라 노션 문서를 손댄 건 이번이 처음이다.

## 1. 이번 세션에 바뀐 규칙

### 폐기 — `항상`·`모든` 단언 금지

2026-08-14에 세웠던 규칙을 작성자 지시로 삭제했다. `HANDOVER.md` 4절 "쓰지 않는 표현" 표에서 그 행을 빼고, 같은 자리에 "폐기된 규칙 — 다시 넣지 마세요"라고 명시해 뒀다. 프로젝트 메모리 `pr-site-sentence-pass.md`와 `kiho-style-observations.md`에도 폐기로 적었다.

사실과 어긋나는 단언을 짚는 일은 계속한다. 규칙이 아니라 사실 확인의 문제다.

### 신설 — 문단 끝 억지 마무리 문장 금지 (Claude 답변·제안 문장)

작성자가 Claude 제안 문단의 마지막 문장을 지목했다. 문제가 된 문장은 이것이다.

> 별도 입력 없이 점프 호 안에서 일어나기 때문에 세 역할이 한 동작에 겹칩니다.

앞 문장들이 이미 다 말한 내용을 다시 묶기만 하는 문장이고, 작성자 표현으로는 "억지로 문단을 맺으려고 만드는 억지 문장"이다. `natural-korean` 스킬 5차 개정에 반영해 `.skill` 파일로 전달했다(저장 여부는 작성자 손에 있다).

- 2절 표에 1행 추가, 그 아래 설명 2문단 신설
- 산출 직전 점검 목록에 8번 추가 — "문단 끝에 앞 내용을 다시 묶기만 하는 문장이 있는가"
- 판별법: 그 문장을 지웠을 때 독자가 잃는 정보가 있는지. 없으면 지운다. 새 사실·조건·결과를 더하면 마지막에 와도 마무리 문장이 아니다

이 규칙은 사이트 본문이 아니라 **Claude가 쓰는 문장**에 걸린다. 제안 문장·채팅 답변·인수인계서 초안이 전부 대상이다.

## 2. `case-shovel.html` 반영 내역

### 핵심 메커닉 — 5항목에서 3항목으로

지난 세션에서 후보만 뽑고 끝났던 블록이다. 작성자가 3항목 압축을 골랐고, 항목 라벨 형식(`<b>라벨</b> — 본문`)을 이 블록에도 들였다. 설계 결정 블록과 같은 형태다.

| 라벨 | 본문 |
|---|---|
| 점프 온리 이동 | 방향 입력이 공중에서만 유효해 지상에서는 좌우 반전만 되고 **실제 이동은 다음 점프의 궤적으로만 일어납니다.** 대시와 패링 포고는 여기서 파생된 확장 이동입니다. |
| 높이로 정한 공격력 | 다운 스트라이크는 공격을 시전한 높이에 비례하여 데미지가 증가합니다. 같은 풀차지 점프라도 정점에서 곧바로 누르면 강 구간이고 하강 도중 뒤늦게 누르면 중 구간으로 떨어집니다. |
| 판정 폭으로 만든 위계 | 다운 스트라이크가 가능한 최소 높이를 설정해서 무제한으로 공격을 연타하는 걸 막는 안전망을 두었습니다. |

**3번은 라벨과 본문이 어긋나 있다.** 내용이 판정 폭에서 최소 발동 높이로 바뀌었는데 라벨은 그대로 두기로 작성자가 골랐다. 다시 묻지 말 것.

빠진 것 — 벽 그랩 없음·천장 충돌, 착지 리커버리 0프레임·코요테 타임·점프 버퍼, 차지/더블/오브젝트 점프 나열. 이 중 **조작 관용(리커버리·코요테·버퍼)은 이제 사이트 어디에도 없다.** 회고 블록이 조작 이야기를 다루니 거기로 한 조각 옮기는 안을 제시해 뒀고 회신은 못 받았다.

### 핵심 루프

첫 줄을 `모든 이동이 점프를 기반으로 일어나고` → `기본 이동은 전부 점프이고`로 바꿨다. 핵심 메커닉 1번과 어휘를 맞춘 것이다.

### 의도한 플레이어 경험 — 여기서 이어받으면 된다

작성자가 **본문을 아예 새로 쓰기로 했다.** 셔블의 핵심 플레이 경험이 뭐냐고 물어서 Claude가 답을 냈고, 그걸 바탕으로 초안 세 개를 제시한 상태에서 대화가 노션 교정으로 넘어갔다. **작성자 회신 대기 중이다.**

Claude가 낸 판단은 "높이 하나로 모든 판단이 묶인다"였다. 근거는 세 규칙이 한 자원을 공유한다는 것 — 점프 높이가 이동 거리를 정하고, 그 높이가 다운 스트라이크의 공격력이 되고, 다운 스트라이크로 적을 맞히는 것만이 잔불을 채운다. 잔불이 없으면 회복도 없다. 그래서 "어디로 갈까"와 "어떻게 싸울까"와 "어떻게 살아남을까"가 분리되지 않는다.

여기서 갈라지는 축이 둘이다. 도망칠 수 없다(체력이 없을수록 적에게 더 가까이 뛰어들어야 한다)와 끊기지 않는 박자(착지 리커버리 0프레임, 바운스로 다음 점프 연결). 현재 사이트 문장의 "통통 튀는 리듬"은 세 번째 축, 결과로 나오는 질감이다. 원인 쪽인 첫 번째를 아직 아무 블록도 말하지 않는다.

제시한 초안은 A안(높이 하나로 묶인 판단), B안(물러설 수 없는 전투), C안(두 문단, 원인과 질감을 다 담고 "통통 튀는 리듬"까지 살림)이다. 세 안 전문은 세션 로그에 있고, 없으면 위 근거로 다시 쓰면 된다.

## 3. 노션 기획서 교정 (SHOVEL 캐릭터 기획서)

작성자 요청 범위는 **맞춤법과 반복 낱말**이었다. 반복 낱말은 작성자가 "의도적인 부분"이라고 판정해 손대지 않았다. 문서 톤은 음슴체를 그대로 쓰기로 했으니 **표 칸의 명사형 종결을 경어체로 바꾸지 말 것.**

### 반영 완료

맞춤법·띄어쓰기 9곳.

- `가시밭이 무한 디딴대가 되면` → `가시밭이 무제한으로 패링 바운스가 되면` (6.8.2, 작성자가 직접 다시 씀)
- `홌드` → `홀드` (6.11 입력 매핑 표)
- `눌리기 쉽어` → `쉬워` (6.11 주석)
- `갭 기준선 자체가 바뀜다` → `바뀜` (8.7 표, 같은 칸 다른 행이 전부 명사형이라 명사형으로 맞춤)
- `방사형 위상의 왜복` → `왕복` (10.2)
- `행동할 수록` → `행동할수록` (5.4)
- `구간 별 발판` → `구간별` (8.2)
- `한 눈에 인지` → `한눈에` (9.2)
- `상부 공간을 활용 할 수 있을` → `활용할` (2.1 이미지 캡션)

문장이 어긋난 곳 6건.

- 5.1 `제공하는 스탯을 핵심적인 요소들만 남겨` → `스탯은`
- 6.2.1 의도 `아슬하게 뛰었는데 목숨이 살아남을 때` → `아슬아슬하게 뛰었는데 살아남을 때`
- 6.3.1 의도 `성취감이 도달의 높이와 비례해서` → `도달 높이에 비례해서`
- 8.2 `높이, 액션 활용을 통한 통제와 설계가` → `높이를 액션으로 통제하고 설계하는 일이`
- 8.9 `도달 불가됩니다` → `도달할 수 없습니다`
- `체수`·`2체` → `마릿수`·`2마리` (5.4 잔불 표, 6.8.2 예외 표 두 칸)

6.5 점프 밟기 블록 두 곳.

- 표에 행 추가 — `더블 점프 카운트 | 리셋하지 않음 — 카운트 리셋은 지면 착지가 전제 (6.4.1)`
- 마리오 참조 주석 교체. 종전 문장이 밟기를 안전망으로만 설명해서 작성자가 "범위가 너무 좁게 적혔다"고 판단했다. 새 문장은 이렇다

> 마리오 시리즈의 밟기 공격에서 영감을 받았습니다. 점프 밟기는 한 가지 역할만 하지 않습니다. 모르고 적 위에 떨어져도 피해를 입지 않는 안전망이자, ATK × 0.5의 약한 공격이고, 1.6타일 바운스와 반사 방향 보정으로 적을 발판 삼아 다음 점프를 잇는 이동 수단입니다.

### 미결 — 표기 흔들림

작성자 판단을 못 받았다.

| 항목 | 상태 |
|---|---|
| `띱니다`(맨 위 인용) / `띕니다`(6.4.2) | 같은 문단인데 어미가 갈림. 맞는 쪽은 `띱니다` |
| `쉐이크`(7.6) 1회 / `셰이크`(9.3) 3회 | 표준은 `셰이크` |
| `모듈러 디자인 블럭`(8.2 제목) | 나머지 20곳은 `블록` |
| `메카닉`(6.4.2·6.7) 2회 | 외래어 표기법은 `메커닉`. 문서에 다른 표기가 없어 통일 여부만 정하면 됨 |
| 숫자 뒤 `타일` 띄어쓰기 | 붙임 29회 / 띄움 96회 |
| `최고 속도`(6.2.1 주석) / `최대 낙하 속도`(같은 절 표) | 같은 값을 두 이름으로 부름 |
| `컨셉` 6회 | 표준은 `콘셉트`지만 업계 관용이라 유지 권장 |

### 미결 — 내용 문제

- **6.11 표 아래 주석이 표와 어긋난다.** 주석은 `다운 스트라이크를 단독 버튼(X)에 두었습니다. 패링(Y)과도`인데, 바로 위 표는 다운 스트라이크가 `↓ + A`, 패링이 `X`다. 예전 키 배치가 주석에만 남은 것으로 보인다
- **2.3 기대효과와 6.5의 연결이 끊겨 있다.** 2.3이 `적을 플랫폼으로 활용해 장애물을 돌파할 수 있습니다`라고 약속하는데, 그걸 이행하는 규칙(밟기 바운스 1.6타일·반사 방향·30° 보정)이 6.5에 있다. 주석에서 2.3을 가리킬지 물었으나 회신을 못 받았다
- 노션이 블록 하나를 안 내려준다(6.6 데미지 구간 표 바로 아래, unknown block). 그 블록은 검수 범위에서 빠졌다

### 반복 낱말 — 작성자 판정 "의도적"

기록만 남긴다. 한 칸 안 반복이 5.4 충전 방법(`충전` 3회·`타격` 3회), 7.4 바닥 착지 행(`파괴` 4회), 6.6.1 의도(`재는` 3회)에 있고, 문서 전체로는 "의도하는 플레이어 경험" 블록 22개가 `감각` 19회·`리듬` 24회·`설계했습니다` 계열 14회로 닫힌다. 맨 위 인용문과 6.4.2 의도 블록은 같은 문단이 통째로 두 번 나온다.

## 4. 다음 순서

`case-shovel` 의도한 플레이어 경험 → 회고(아직 검토 전, Claude가 쓴 문장 그대로) → `case-yeongsan` → `case-bloom` → `case-subbrain` → `case-redteam` → `contact.html`.

각 파일에서 미리 잡아 둔 것은 `HANDOVER-2026-08-21.md` 5절에 그대로 있다. 요약하면 영산·블룸 루프 설명의 연결어미 뒤 쉼표, 블룸의 접근/설계 블록과 핵심 루프 중복, sub_brain의 `쓰는 법` 라벨, 케이스 5종 공통 틀(부제 떼고 라벨만) 적용이다.

셔블에 남은 미결도 그대로다. 설계 결정 3번(`레벨 디자인의 수직 폭`)에서 사다리·엘리베이터를 빼면서 인과 한 단계가 생략됐고, 조작 관용 논점을 회고로 옮길지 정하지 않았다.

## 5. 작업 환경

- 노션 편집은 `notion-update-page`의 `update_content`로 했다. `old_str`이 정확히 한 번 매칭돼야 하고, 표 행 추가는 `<tr>\n<td>...</td>\n<td>...</td>\n</tr>` 형태를 그대로 넣으면 된다(6.5 표에서 확인함). 편집 뒤에는 다시 fetch해서 검증할 것 — 페이지가 커서 결과가 파일로 저장되니 grep으로 확인하면 된다
- 사이트 파일은 `device_bash`로 마운트된 폴더에서 바로 고쳤다. `device_stage_files` → `/tmp` 복사 절차보다 빠르다
- git 커밋은 여전히 없다. 체크포인트 `dd82e64` 이후 수정분이 전부 작업 트리에 쌓여 있다. 이 세션에서 git을 쓰면 잠금 파일이 남고 삭제 권한이 없으니 **작성자가 데스크톱에서 직접 커밋하는 편이 안전하다**

## 6. 판단 대기 (누적)

`2023 졸업`/`2023년 졸업` 불일치, 보조용언 붙여쓰기 혼재, about-opener 마침표, GIGDC 풀네임 위치, `ai.html` 헤더 문장, 영문 `Game System Designer` 잔존, 7.0 이력 카드 형식 불일치, about 5.0의 `kiho-writing-ko` 잔존 여부, 4.0에서 뺀 `간단한` 되살릴지, about 6.0 고등학교 첫 문장 사실 확인.

자산 미해결 — 없는 이미지 `sub-brain-graph.png`·`redteam.png`·`genshin.png`·`blue-archive.png`(전부 onerror로 동작), 안 쓰이는 `sdorica.png`·`shovelhopping.gif`·`docs/shovel-character-v5.pdf`, 색 강조 태그가 span/strong/b 세 형태.
